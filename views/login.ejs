<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/styles/style.css">
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-custom bg-cover bg-center h-screen w-full flex items-center justify-center px-5">
   <div class="absolute bottom-0 w-full bg-opacity-50 p-5">
       <h1 class="text-2xl text-white font-semibold"><span class="text-orange-600">Enter the name</span> By which you call them</h1>
       
       <!-- Display flash message -->
       <% if (message.length > 0) { %>
           <div class="bg-red-500 text-white p-2 rounded mb-4 text-center fixed top-10 left-0 right-0 transition-transform transform -translate-y-full animate-slide-down">
               <%= message %>
           </div>
       <% } %>

       <form action="/login" method="post">
           <div class="my-5 relative">
               <input type="password" name="pass" id="password" class="bg-stone-400 rounded-lg p-2 px-4 w-full text-lg" placeholder="Password">
               <i id="togglePassword" class="ri-eye-fill text-stone-900 text-2xl absolute right-2 top-2"></i>
           </div>
           <div class="flex items-center justify-center">
               <button type="submit" class="bg-stone-200 hover:bg-blue-700 text-stone-900 font-bold py-3 px-4 rounded-xl focus:outline-none focus:shadow-outline w-full">
                   <h1 class="text-lg">Get Verified</h1>
               </button>
           </div>
       </form>
   </div>
   <script src="/scripts/script.js"></script>
</body>
</html>
